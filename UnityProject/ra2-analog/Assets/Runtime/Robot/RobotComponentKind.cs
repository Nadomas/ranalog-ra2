namespace Ra2.Robot
{
    /// <summary>S3-01 component kinds for data-driven assembly (expand via data, not core rewrites).</summary>
    public enum RobotComponentKind : byte
    {
        Chassis = 0,
        Wheel = 1,
        NoseMarker = 2,
        Module = 3
    }
}
